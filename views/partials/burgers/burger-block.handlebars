<li>
	{{burger_name}}

	<button class="change-sleep" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Eat it!{{else}}Mmmm!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete it!</button>
</li>